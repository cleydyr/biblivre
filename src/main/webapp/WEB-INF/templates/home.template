<!DOCTYPE html>
<html xmlns:th="http://www.thymeleaf.org">

<head>
    <meta charset="UTF-8">
    <title th:text="${title}">Example Page</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css">
</head>

<body>
    <div class="container mt-5">
        <h1 th:text="${title}">Example Page</h1>
        <p th:text="${message}">Welcome to the example page!</p>

        <div class="card mt-4">
            <div class="card-body">
                <h5 class="card-title">Example Content</h5>
                <p class="card-text">This is a simple example page created with Spring MVC and Thymeleaf.</p>
            </div>
        </div>
    </div>
</body>

</html>