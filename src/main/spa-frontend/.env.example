# Copy to .env.local and adjust. Vite bakes `VITE_*` at dev/build time.

# Enable the Excel export button in the SPA (also set JVM env
# BIBLIVRE_FEATURE_SEARCH_EXCEL_EXPORT=true or the API returns no permission).
VITE_FEATURE_SEARCH_EXCEL_EXPORT=false
